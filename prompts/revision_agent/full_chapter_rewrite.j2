{% if config.ENABLE_LLM_NO_THINK_DIRECTIVE %}/no_think{% endif %}

Rewrite Chapter {{ chapter_number }} featuring protagonist {{ protagonist_name }}.
Genre: {{ genre }}

Revision reasons (address ALL):
{{ revision_reason }}

{{ all_problem_descriptions }}

{{ deduplication_note }}

{{ length_issue_explicit_instruction }}

{{ plan_focus_section }}

Canon context (do not contradict):
{{ hybrid_context_for_revision if hybrid_context_for_revision.strip() else "No previous context." }}

Original draft (reference only; do not copy):
{{ original_snippet }}

Instructions:
- Rewrite the entire chapter as fresh prose.
- Address every listed issue explicitly through the rewrite.
- Maintain continuity with the canon context.
- Output ONLY the rewritten chapter prose.
- No chapter header, no title, no analysis, no lists, no code fences.
