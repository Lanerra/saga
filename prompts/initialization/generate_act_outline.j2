## Story Information

Title: {{ title }}
Genre: {{ genre }}
Theme: {{ theme }}
Setting: {{ setting }}
Protagonist: {{ protagonist_name }}

## Act

Act: {{ act_number }} of {{ total_acts }}
Role in structure: {{ act_role }}
Chapters in act: ~{{ chapters_in_act }}

## Global outline (context)
{{ global_outline }}

## Main characters (context)
{{ character_context }}

## Task

The JSON root MUST be an object with EXACTLY these keys:
- "act_number" (int): MUST equal {{ act_number }}
- "total_acts" (int): MUST equal {{ total_acts }}
- "act_role" (str): MUST equal "{{ act_role }}"
- "chapters_in_act" (int): MUST equal {{ chapters_in_act }}
- "sections" (object)

The "sections" object MUST have EXACTLY these keys:
- "act_summary" (str)
- "opening_situation" (str)
- "key_events" (array of 5 to 7 objects)
- "character_development" (str)
- "stakes_and_tension" (str)
- "act_ending_turn" (str)
- "thematic_thread" (str)
- "pacing_notes" (str)

Each object in "key_events" MUST have EXACTLY these keys:
- "sequence" (int): starts at 1 and increases by 1
- "event" (str): an on-page, concrete event
- "cause" (str): what directly causes the event
- "effect" (str): what directly changes because of the event

Constraints:
- Stay consistent with the global outline.
- Use named characters and concrete locations.
- No meta-commentary.
- Do not include any keys not listed above.
- Do not wrap the JSON in any surrounding text.

CRITICAL OUTPUT CONTRACT:
- Return ONLY valid JSON (the entire response must be JSON; no surrounding text).
- Return a single JSON object at the top level.
- Do NOT wrap the object in an array or another object.
