{% if config.ENABLE_LLM_NO_THINK_DIRECTIVE %}/no_think{% endif %}

Summarize Chapter {{ chapter_number }} in 1-3 sentences focusing on what changed or was revealed.

CRITICAL OUTPUT CONTRACT:
- Return ONLY valid JSON.
- Output MUST be a single JSON object with exactly one key: "summary".
- No markdown. No code fences. No extra text.

Chapter text:
{{ chapter_text }}
