Extract structured character information from this character sheet.

Character Name: {{ name }}

Character Sheet:
{{ description }}

Extract the following:
1) Traits: list 3-7 single-word personality traits (examples: "brave", "cynical", "impulsive")
2) Status: their current state (examples: "Active", "Deceased", "Missing")
3) Motivations: what drives them (1-2 sentences)
4) Background: brief history (1-2 sentences)

Output contract (STRICT JSON, no exceptions):
- Output MUST be valid JSON only.
- Output MUST be a single JSON object with EXACTLY these keys:
  - "traits" (array of strings)
  - "status" (string)
  - "motivations" (string)
  - "background" (string)
- Do NOT output markdown, code fences, or commentary.
- Do NOT include any extra keys.

Example:
{"traits":["brave","loyal","determined"],"status":"Active","motivations":"Protect the innocent and restore honor.","background":"Trained as a knight from childhood."}